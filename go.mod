module github.com/JlovesYouGit/termux-crush

  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build !android  //go:build !android
  // +build